# JadeOS — PRD

## Original problem statement
Build an AI agent platform to sell to companies in the Minneapolis area, with a full business plan and launch package to court existing businesses. Six agent types (customer service / Tier-1 support, sales qualification & lead scoring, data extraction & processing, ops / workflow automation, content / email generation, on-call ops co-pilot) wrapping Claude / GPT.

After 1st pivot: app must be **universal** — not freight-only. Agents must serve any industry (freight, logistics, manufacturing, healthcare, SaaS, e-commerce, insurance, legal, real estate, professional services, general) and tailor lexicon + schema + tone per industry.

After 2nd pivot (2026-02): **Trinity positioning for VCs** — pitch three products in tandem:
1. **JadeOS Quantum AI** (flagship · 50+ modules · voice-first "Hey Jade" · 128-qubit Qiskit Aer + Claude Haiku 4.5)
2. **JadeOS-Agent Suite** (six freight-vertical agents · sits on top of any TMS)
3. **Hot Shot TMS** (operator-built system of record for the underserved hot-shot segment)

Umbrella brand: **JadeOS** (mixed-case). Voice command "Hey Jade" preserved. Founder email `founder@jadeos.ai` preserved.

## Architecture
- **Frontend**: React (CRA + craco), Tailwind, shadcn/ui, JADE OS brand (Space Grotesk + Inter + JetBrains Mono, Jade Lime / Quantum Cyan / Vault Violet on Console Black).
- **Backend**: FastAPI + Motor (MongoDB). All routes prefixed `/api`. JWT admin auth (bcrypt + PyJWT).

## Latest changes (2026-02-08)
- **Lighthouse Member Audit + Dimension Analyst Breakdown (2026-02-08)** — open self-service audit for Lighthouse cohort, end-to-end + per-dimension drill-down:
  - **Backend (`consulting_audit.py`)** — new `POST /api/audit/lighthouse/start` (public · industry-agnostic across 11 verticals) + `GET /api/audit/lighthouse/{audit_id}/dashboard` (read-only member view). `audit_id` IS the access token (Notion/Calendly-style share link · no admin). PDF generator extended: when `lead_magnet="lighthouse_member"` it inserts 2 extra pages → page 10 "Founding-Customer Perks" (6 perks) and page 11 "Personalized Pilot Terms" (9 personalized terms computed from the customer's score: anchor agent, focus dimension, success metrics, payback, exit memo, etc.). Total: 14 pages for Lighthouse, 12 for standard. Pipeline stage → 7-stage Lighthouse timeline mapping (applied · audit_started · audit_analyzed · pilot_kickoff · pilot_midpoint · pilot_complete · case_published).
  - **`GET /api/audit/{id}/explain/{dimension_id}`** — senior-analyst drill-down for any single dimension score: HEADLINE · WHAT N/100 ACTUALLY MEANS · DRIVERS (citing actual 1-5 answers) · DAY-TO-DAY CONSEQUENCES · AI LEVERAGE · FIRST MOVE. Claude Sonnet 4.5 with deterministic fallback grounded in responses. Cached on the audit doc.
  - **Frontend** — `/lighthouse/audit` (LighthouseAuditLanding.jsx · industry selector · 6 perks panel) + `/lighthouse/member/:auditId` (LighthouseMemberDashboard.jsx · 7-stage timeline · 4 summary cards · founder contacts · deliverables grid · founding-customer perks). AuditPage results: dimension rows are now clickable buttons → opens DimensionModal with the analyst breakdown. PDF download button shows correct page count. Home page Lighthouse band gets a third "MEMBERS · RUN YOUR AI READINESS AUDIT" CTA. Dedicated Lighthouse member section under `/lighthouse` hero.
  - **Verified** — full flow tested via curl: start → respond (29 Qs) → analyze (Tier CURIOUS · 48.1/100) → explain DATA (LLM-generated breakdown citing actual answers "Mostly paper / email" + "Stale · weekly+") → 14-page PDF renders with zero margin violations (`pdfplumber`) → member dashboard returns 7-stage timeline. Standard 12-page PDF regression: PASS. Screenshots show all three pages rendering correctly.
- **Promo videos · VC-ready text reveal (2026-02-08)** — Sora-generated wordmark tails (which rendered "JADE OSI", "ONEJADES.COMN", "IJADES" due to Sora's unreliable text rendering) trimmed (last 3.0s) and replaced with a deterministic ffmpeg `drawtext` "JadeOS · TRINITY · QUANTUM AI · AGENT SUITE · HOT SHOT TMS · ONEJADES.COM" brand reveal card (3.0s · pixel-perfect text). New `build_wordmark_card()` in `polish_promo_videos.py`. All three videos re-encoded to identical 1920×1080/30fps/H.264 CRF 18/AAC 192k/stereo·48kHz, ~16.6s. System `ffmpeg` (libfreetype + drawtext) installed.
- **PDF text-overflow fix · validated (2026-02-08)** — `consulting_audit.py` `wrap_to_width()` confirmed via stress-test on 12 + 14 pages. Regression test: `/app/backend/tests/test_audit_pdf_layout.py`.
- **Pipeline Kanban · visual validation (2026-02-08)** — `/admin` PIPELINE KANBAN tab verified end-to-end. No regressions.


## Earlier changes (2026-02)
- **AI Readiness Audit · feature complete** — full consulting engine across backend + frontend + admin:
  - **Backend (`/app/backend/consulting_audit.py`)** — 6 universal dimensions (DATA / PROCESS / TOOLS / CHANGE / ROI / TECH) with 4 weighted questions each + 4-6 industry-specific KPIs across 11 industries (freight_brokerage, logistics, manufacturing, healthcare, saas_tech, e_commerce, insurance, legal, real_estate, professional_services, general). Deterministic scoring → tier (PIONEER / BUILDER / CURIOUS / LEARNING) with Claude Sonnet 4.5 narrative synthesis (deterministic fallback if LLM unavailable). 12-page PowerPoint-style PDF via reportlab matching pitch-deck visual language. Endpoints: `POST /api/audit/start` · `GET /api/audit/{id}/questions` · `POST /api/audit/{id}/respond` · `POST /api/audit/{id}/analyze` · `GET /api/audit/{id}/report.pdf` · `GET /api/audit/dimensions` · `GET /api/admin/audits` · `DELETE /api/admin/audits/{id}`.
  - **Frontend (`/app/frontend/src/pages/AuditPage.jsx`)** — single-page wizard with three modes (start / wizard / results). Stunning results dashboard with overall-score gauge, SVG radar chart, dimension bar breakdown, recommended agents, 90-day pilot proposal block, ROI tile, risk register, "Next 30 Days" action list, 12-page PDF download.
  - **Admin (`/app/frontend/src/components/AuditAdminPanel.jsx`)** — list of every audit ever run with KPIs (total · analyzed · avg score · annual pipeline $), tier breakdown bar, industry + status filters, click-through to results, PDF re-download, delete.
- **Free 90-Day Broker Audit landing (`/audit/broker-free`, `/free-audit` alias)** — public lead-magnet page for freight brokers. Hero "Free AI Readiness Audit · For Freight Brokers", three value cards (12-page report / 30 minutes / $0), 8-item data checklist visible on page + downloadable PDF, email-gated start form that creates a freight_brokerage audit pre-tagged `lead_magnet: "free_90_day"` for admin filtering.
- **JadeOS Audit Playbook PDFs (`/app/backend/audit_playbook.py`)** — four auto-generated operator assets accessible at `/audit/playbook`:
  - Data Checklist · 1-pager (8-item packet to send the prospect)
  - Audit Playbook · 6-page (how to run the 30-question audit live · prep, talk track per dimension, scoring rubric, objection handling, post-audit handoff)
  - Data Request Letter · drop-in client email template
  - Pilot Engagement Agreement · 1-page SOW with named success metrics
- **Outreach Campaign Engine (`/app/backend/outreach_campaigns.py` + `/app/frontend/src/components/OutreachPanel.jsx`)** — admin tab with 5 pre-baked templates: broker_cold_email · lighthouse_trio · consulting_upsell · linkedin_dm · followup_sequence. Variable substitution ({{first_name}}, {{company}}, {{score}}, {{tier}}, etc.). Render preview · copy-to-clipboard · log-send tracker · status patches (sent → replied → meeting_booked → passed). KPI strip on top.
- **Tested** — testing agent iteration_14: backend 100% (12/12 pytest), frontend 100% (all critical flows verified via Playwright), 0 critical issues. Minor login-redirect-preserves-?tab= UX issue fixed by adding `?next=` param in Login.jsx + Dashboard.jsx redirect.
- **Promo videos polished for VC delivery** — all three (v1/v2/v3) regenerated to 1920×1080 / 30fps / H.264 CRF 18 / AAC 192k stereo 48 kHz / 16.5s each, custom JadeOS AI Suite intro card + outro contact card, fade-in/out on both video and audio.
- **JadeOS Quantum AI · /demo tab (P0 complete)** — flagship product is now click-through for VCs. New tab placed BEFORE Hot Shot TMS preview, with four sub-panels: VOICE · HEY JADE (live Claude stream with deterministic fallback), QUANTUM · 128 QUBITS (4 circuits: Bell, GHZ, Grover-2, QFT-3 — math-correct distributions, Qiskit-Aer-compatible architecture, no heavy dep), MODULES · 50+ (54 curated flagship modules across 8 categories with ship-status badges + filter chips), MEMORY · PERSISTENT (sample thread with 5-category facts ledger + append-only turns tape). New files: `/app/backend/quantum_demo.py` (router + 54 modules + 4 circuit simulators + memory preview), `/app/frontend/src/components/JadeOSQuantumPanel.jsx`. Wired into `AgentDemo.jsx` (tab strip + URL allowlist + content). Deep-link `/demo?tab=quantum` works. `/invite` hero adds a 4th CTA `▶ JADEOS QUANTUM AI · PREVIEW`. Backend pytest 6/6 (`tests/test_quantum_demo.py`); testing agent: 100% backend + 100% frontend, zero issues.
- **Trinity rename** — flagship `Jade` → `JadeOS Quantum AI`; agent suite `JADE OS` → `JadeOS-Agent Suite`; umbrella brand → `JadeOS` (mixed-case). Applied across `InvestorInvite.jsx`, `Dashboard.jsx`, `HotShotTmsPanel.jsx`, `ops_workbench.py` (PITCH_DECK + TECHNICAL_DOC + plan PDF), `Nav.jsx`, `Footer.jsx`. Preserved: `Hey Jade` voice command, `Jade Pro` / `Jade Broker tier` / `Jade Schools` / `Jade Maker` sub-tiers, `founder@jadeos.ai` email. PDF cover, page 4 (solution slide) verified to render the three product names correctly.
- **Backend syntax fix** — removed orphan duplicate `"slides"/fact_sheets/readiness/competitive_brief` block (old 2-product copy) that was left after the previous session's `search_replace` edit in `ops_workbench.py`. Backend was failing to start with `IndentationError` at line 948 before this fix.
- **Verification** — `/api/agent/workbench/architecture` 200 · `/api/agent/workbench/deck.pdf` 200 (16.9KB · 12 pages, cover + slide 4 verified via pypdf text extract) · `pytest tests/test_iteration12_workbench.py` 10/10 passing · ESLint clean on `InvestorInvite.jsx` · screenshot of `/invite` confirms hero, three-product card grid, and nav all show the renamed brands.

- **LLM**: Claude Sonnet 4.5 + GPT-5.2 via `emergentintegrations.LlmChat` with Emergent Universal Key. Streaming SSE on chat.
- **Industry-aware system prompts**: `_system_for(industry, role_hint)` builds prompts using an `INDUSTRY_LEXICON` dict.

## User personas
- Operator (ops director, dispatcher, CS lead, COO) at MSP-area SMB/mid-market across any of 11 industries.
- Admin (JADE founder) — reviews leads, watches agent run tape.

## What's implemented (2026-02)
- **OPERATIONS WORKBENCH · 6 LABS · DECISIONS · RISKS · 8 PHASES (2026-02-06, iter 12)** — completes the user's "Operations Workbench" P0 request. **20/20 pytest pass · iteration 12 backend 95% (now 100% post-fallback) · frontend 100%**. Backend `/app/backend/ops_workbench.py` defines 6 OPERATIONS, 5 DECISIONS, 7 RISKS, 8 PHASES, plus generators for 3 PDFs (`generate_market_analysis_pdf`, `generate_pitch_deck_pdf`, `generate_technical_doc_pdf`). All 6 Labs are now FULL depth:
    - **OP-01 · MARKET_ANALYSIS** — LLM-authored 7-section deep-dive PDF (Claude Sonnet 4.5, JSON-mode). **Robust fallback**: 45s LLM timeout → deterministic curated `MARKET_ANALYSIS_FALLBACK_SECTIONS` so the PDF ALWAYS lands (verified · `source=deterministic_fallback` when LLM unavailable; both paths produce real %PDF files).
    - **OP-02 · FINANCIAL_MODELING** — `build_roi_model(archetype, fleet_size)` · pure math over public benchmarks (ATA · ATRI · BLS · EIA · FMCSA · NICB). 3 archetypes (small_regional · mid_market · specialized_hazmat). Returns 6-category savings, 3-yr NPV @ 10% discount, payback months, sensitivity ±10%, sources list.
    - **OP-03 · AI_SYSTEMS_ARCHITECTURE** — 6 agent modules (Dispatch · Route+Fuel · Compliance · Pricing · Retention · Maintenance), each with inputs / outputs / decision-logic / autonomy / KPI. 6-stage data pipeline, 5-lane swimlanes, 6-endpoint API surface.
    - **OP-04 · SALES_COLLATERAL** — 12-slide pitch deck (Title · Problem · Solution · 3 module deep-dives · ROI · 2 case profiles · Competitive · Pricing · Ask), 4 persona fact-sheets, 3-area readiness assessment, 4-competitor brief. Generates a landscape-letter PDF (~10KB).
    - **OP-05 · DOCUMENTS** — 10-section operator-grade technical brief (Exec Overview · 6 module deep-dives · ROI case studies · Roadmap · MN opportunity + technical appendix). Generates a portrait-letter PDF (~9KB) with all numbers benchmark-traceable.
    - **OP-06 · BUSINESS_RESEARCH** — seeds the curated FMCSA-verified MN freight roster (idempotent upsert), returns 50 real companies with DOT#/MC#.
    - **DECISIONS TRACKER · 5 auto-seeded** (D1 flagship-vs-suite, D2 direct-vs-indirect channel, D3 POC investment cap, D4 MN-custom-vs-national APIs, D5 dedicated FTE vs partner-led) — clickable option chips persist + audit.
    - **RISKS REGISTER · 7 auto-seeded** with severity + status; one-click MITIGATED/ACCEPTED.
    - **8-PHASE PLAN** with steps + per-step status mutation.
    - **Admin endpoints** (require_admin): `/api/workbench/overview`, `/api/workbench/labs/op-{01..06}/run`, `/api/workbench/labs/op-{01,04,05}/download/{run_id}`, PATCH `/api/workbench/decisions/{id}`, PATCH `/api/workbench/risks/{id}`, PATCH `/api/workbench/phases/{n}/steps`, GET `/api/workbench/labs/{op}/runs`.
    - **Public Console wrappers** (no admin gate, used by `/demo` ConsoleWorkbenchPanel): `GET /api/agent/workbench/architecture`, `POST /api/agent/workbench/roi`, `GET /api/agent/workbench/collateral`, `GET /api/agent/workbench/document`, `GET /api/agent/workbench/deck.pdf`, `GET /api/agent/workbench/document.pdf`, `GET /api/agent/workbench/{decisions,risks,phases}`.
    - **Frontend** · admin `WorkbenchPanel.jsx` (6 Lab dispatcher · 8-phase view · decisions tracker · risks register · materials + tools) wired as the new first tab in `/admin`. Public `ConsoleWorkbenchPanel.jsx` (5 modes · ROI / Architecture / Collateral / Brief / Strategy) wired as new "OPS WORKBENCH" tab in `/demo` — exposes the Workbench to non-admin viewers per user's "agents in the console can perform as many features outlined" directive.

- **TRUCKER AI · driver-grade ops with verifiable-only data (2026-02-06)** — every endpoint returns source attribution; no mock data anywhere. Iteration 11 passed 12/12 backend + frontend 100%. Backend `/app/backend/trucker_ai.py` ships five live integrations against FREE PUBLIC sources (no API keys required):
    - **HOS calculator** · 49 CFR §395 codified locally (11/14/30/70 federal rules); endpoints `/api/trucker/hos-rules` + `/api/trucker/hos-check`. Returns specific violations cited to §395 paragraph.
    - **Truck stops / fuel / weigh stations / rest areas** · OpenStreetMap Overpass API; `/api/trucker/truck-stops`. Filters by amenity=truck_stop, fuel+hgv=yes, highway=rest_area, weighbridge. Each result links back to openstreetmap.org/{type}/{id}.
    - **Geocoding + routing** · OpenStreetMap Nominatim + OSRM public router; `/api/trucker/geocode` + `/api/trucker/route`. Routing returns distance/duration + EXPLICIT warning that the auto profile must be cross-checked against state DOT 511 for truck restrictions.
    - **Weather** · NOAA api.weather.gov; `/api/trucker/weather`. 6-period forecast per point.
    - **50-state DOT 511 directory** · curated; `/api/trucker/state-511/{state}` + `/api/trucker/state-511`. Every URL verifiable.
    - **Diesel prices** · EIA v2 API scaffold — returns source=eia_scaffold when EIA_API_KEY empty; live on key drop.
    - Frontend `TruckerAIPanel.jsx` (5 sub-views: HOS / Parking / Route / Weather / 511). Wired into BOTH the admin dashboard (new TRUCKER · DRIVER OPS tab) AND the public Console at /demo. Public Console also gets a new live `SystemStatusStrip` showing real LLM provider status + UTC clock + tick counter.

- **REAL LEADS · FMCSA-seeded MN freight + CSV bulk import (2026-02-06)** — replaced the "fake company" problem in PROSPECTS panel. Iteration 10 passed. Backend `/app/backend/fmcsa_lookup.py` ships a curated registry of 14 real verifiable MN freight companies (every DOT#/MC# checks out at safer.fmcsa.dot.gov). Endpoints `/api/leads/seed-real-mn` (idempotent), `/api/leads/import-csv` (validates email format + domain resolves), `/api/leads/{id}/enrich-fmcsa` (live FMCSA QCMobile lookup, scaffolded — drop FMCSA_WEBKEY to activate), `/api/leads/{id}/tailor-hook` (LLM uses ONLY verifiable facts — refuses synthetic leads with HTTP 400). All existing AI-generated prospects backfilled `is_synthetic=true` on startup; UI defaults to hiding synthetic with a clear ⚠ banner. Frontend in `Dashboard.jsx ProspectsPanel` rewrite: SEED REAL MN button, CSV import form with header parsing, source filter (real/synthetic/all), VERIFIED + SYNTH + DOT# badges per row, TAILOR HOOK button (works only on real leads).

- **RISK GUARD · pre-quote validation + audit chain + alerts (2026-02-06)** — the "don't let the agent quote $800 on a $1,200 load" defense. Iteration 9 passed 26/26 backend specs + 100% frontend. P0 contract verified end-to-end: a quote at or below cost_basis_usd ALWAYS returns severity=CRITICAL + decision=HARD_BLOCK, and HARD_BLOCK can only be cleared by `/api/quote-reviews/{id}/override` with non-empty written notes (which land in the immutable audit chain).
    - **Rate Floors · 3-source layered** (`/app/backend/rate_floors.py`): manual (operator-set per lane+equipment) + formula (`carrier_pay × (1+margin) + fuel`) + historical (P25 of last 90 days' actuals, ≥3 samples). Highest floor wins; all candidates persisted for audit reproducibility. Endpoints `/api/rate-floors` (POST/GET/DELETE), `/api/rate-actuals` (POST records historical).
    - **Quote Validation engine** (`/api/quotes/validate`): severity bands LOW (0-5%), MEDIUM (5-10%), HIGH (10%+), CRITICAL (≤ cost_basis); decision matrix CLEAR/LOW→AUTO_OK, MEDIUM/HIGH→QUEUE_REVIEW, CRITICAL→HARD_BLOCK. Every validation persists a `quote_reviews` row + audit event regardless of outcome. SLA timer per severity.
    - **Human Review queue** (`/api/quote-reviews/*`): approve / reject / override. Override is the only way to clear a HARD_BLOCK and requires non-empty `notes` (400 without). Notes flow into the audit chain.
    - **Immutable Audit Trail** (`/app/backend/audit_trail.py`): SHA-256 hash chain — every event includes hash of the prior event. `/api/audit/events` lists/filters, `/api/audit/verify` re-hashes the entire chain and returns ok=true/false. Forever retention (AUDIT_RETENTION_DAYS=0).
    - **Alerts subsystem** (`/api/alerts/*`): every QUEUE_REVIEW or HARD_BLOCK persists to `db.alerts` + fires every active webhook with `kind="alerts"` (new Literal) + queues an email via Resend (or queues for later flush if Resend not wired). Admin can ack individual or all via `/api/alerts/ack-all`.
    - **Frontend `RISK GUARD · PRE-QUOTE` tab** (`/app/frontend/src/components/RiskGuardPanel.jsx`): 5 sub-views — validate bench (live test), reviews queue (approve/reject/override with prompt-required notes), floors CRUD, alerts queue with ack-all, audit timeline with chain verify button.

- **WORKFLOW MEMORY · per-topic threads + auto-distillation (2026-02-06)** — full agent memory for freight workflows. Iteration 8 passed 16/16. Backend `/app/backend/memory_workflow.py` + endpoints in `server.py` (`/api/memory/threads`, `/turns`, `/distill`, `/recall`, PATCH). Threads scoped on (thread_type, thread_key) where thread_type ∈ {load, customer, issue}. Every turn (operator / agent_action / user / assistant / system) appends to `db.memory_turns`. Distillation runs every 6 turns (env: MEMORY_DISTILL_EVERY_TURNS) and produces a five-category fact ledger: HAPPENED · DECIDED · OPEN QUESTIONS · RISKS · NEXT ACTIONS. `/api/agent/chat` now optionally binds to a memory thread via `memory_thread_type` + `memory_thread_key` — when bound, the recall block is auto-injected into the system prompt and both sides of the exchange are auto-appended. Frontend `MemoryThreadsPanel.jsx` renders thread list (filterable by type), thread detail with facts ledger + transcript + composer + force-distill / close / archive controls.

- **ACTIVE CLAIMS FILING · cargo / detention / OS&D (2026-02-06)** — agent autonomously drafts and files freight claims. Backend `/app/backend/claims.py` + endpoints `/api/claims/draft` (LLM-pack from memory thread + context), `/api/claims` (POST persists, with `auto_file` boolean), `/api/claims/{id}/file` (operator-approved fire), GET/PATCH/DELETE. Mixed autonomy: `CLAIMS_AUTO_FILE_LIMIT_USD=500` (env-tunable) — claims at or under auto-file immediately, higher value queue as `ready_for_review`. Filing delivers payload to every active webhook with `kind="claims"` (new literal added to WebhookConfig). Memory thread gets an `agent_action` turn for every claim created/filed so the workflow remembers what JADE did. Frontend `ClaimsPanel.jsx` has the LLM draft wizard (pick memory thread + optional context + auto-file toggle), the queue with kind/status filters, and one-click FILE buttons on review-mode rows.

- **INTEGRATIONS SCAFFOLDS · Sentry · RAG · Client Auth · Resend (2026-02-06)** — shipped all four wire-in stubs per user directive "skip all and wire all in for easy integration later". Iteration 7 testing: 8/8 specs pass; backend + frontend at 100%.
    - **Sentry** (`/app/backend/integrations/sentry_stub.py`, `/app/frontend/src/lib/sentry.js`) — `init_sentry()` / `initSentry()` no-op when DSN unset. Activate by dropping `SENTRY_DSN` + `REACT_APP_SENTRY_DSN`. Backend integrates FastAPI+Starlette via `sentry-sdk`; frontend uses `@sentry/react` (dynamic import).
    - **True RAG · per-tenant** (`/app/backend/integrations/rag_store.py`) — `VectorStore` abstract interface + `MemoryVectorStore` (token-overlap scoring). Per-tenant buckets — query against tenant_id='other' returns 0 hits even when content exists for 'acme-freight'. Endpoints: `POST /api/rag/ingest` (admin), `POST /api/rag/query` (public — used by future agent runtime), `GET /api/rag/tenant/{id}`, `DELETE /api/rag/tenant/{id}/{doc_id}`. Docs mirrored to `db.rag_docs` for durability. Drop `VECTOR_STORE_PROVIDER=pgvector|pinecone|qdrant` + `OPENAI_API_KEY` to upgrade.
    - **Client Portal · Magic Link Auth** (`/app/backend/integrations/client_auth_stub.py`, `/app/frontend/src/pages/ClientLogin.jsx`, `ClientDashboard.jsx`, `/app/frontend/src/lib/clientAuth.js`) — `/client/login` → magic link minted (15-min TTL) → `/client/verify?token=...` → JWT (`aud=client`, 24h TTL) → `/client/dashboard` (read-only). Routes: `/api/client/auth/request-magic-link`, `/api/client/auth/verify`, `/api/client/me`. In preview mode (no Resend), magic URL is returned in the response so operator can click manually. Tokens have `aud=client` so they cannot be confused with admin JWTs.
    - **Resend** — already wired; surfaced status via new admin endpoint. Drop `RESEND_API_KEY` and queued auto-followups + magic-link emails fire automatically.
    - **Admin tab `INTEGRATIONS · WIRED`** (`/app/frontend/src/components/IntegrationsScaffoldPanel.jsx`) — single source of truth showing all 4 scaffold cards with "AWAITING KEY" / "WIRED · LIVE" badge + activation hint + live RAG test bench (ingest + tenant-isolated query). Endpoint: `GET /api/admin/integrations-scaffold`.

- **OPERATIONS · LIGHTHOUSE PROGRAM (2026-02-05)** — full 5-client operating playbook shipped. Backend `/app/backend/operations.py` encodes team roster + monthly burn, infrastructure cost matrix, SLA tiers (P1/P2/P3), 5-phase onboarding playbook, roadmap prioritization matrix, year-1 financial model, 12-month milestones. Endpoint: `GET /api/admin/operations`. Per-client P1/P2/P3 ticket CRUD under `/api/admin/pilot-tickets` (list/post/patch/delete). Frontend `OperationsPanel.jsx` renders all of it as a glanceable operating dashboard.
- **COMPLIANCE · ROUTE TAB (2026-02-05)** — new admin tab answering the operator's most pressing GTM question: "which verticals can I sell to right now, and which need certification first?". Backend `/app/backend/compliance.py` encodes 14 industry rows with status (`go_now` / `go_with_tos` / `blocked`), gates with cost estimates, must-have ToS clauses, MSP-region market size, unlock paths, and headline principles. 6 universal requirements (Privacy Policy, ToS, DPA, AI Disclosure, SOC 2 Type II, E&O Insurance) and a 4-phase 8-month roadmap. Endpoints: `/api/admin/compliance` (full data) + `/api/compliance/public` (status-only slice for marketing). Frontend `CompliancePanel.jsx` renders pills, filterable IndustryCards, universal requirements table, and roadmap cards. Industry metadata also propagated to `/app/frontend/src/lib/industries.js` so the public demo can flag gated verticals.
- **HEALTH · DIAGNOSTICS TAB + LLM ERROR CLASSIFIER + LAUNCH CAMPAIGN (2026-02-05)** — 3 major capabilities shipped & verified (29/29 tests passing):
    - **LLM error classifier**: `classify_llm_error()` detects budget_exceeded / insufficient_balance / rate_limited / auth_failed / timeout / generic. Returns typed 402 (`detail.code='budget_exceeded'`) so frontend can act on it. SSE chat stream embeds the same code. Every failure persists to `db.llm_errors` for trend tracking.
    - **HEALTH tab** (`HealthPanel.jsx`): overall status pill, ACTION REQUIRED list, LLM budget meter ($current/$max with color-coded progress bar), services config matrix, security posture readout, Mongo/disk metrics, auto-followup queue, scrollable LLM error stream with per-code dedup, self-repair buttons (retry followups, purge stale runs, clear errors). Auto-refreshes every 30s. Live `PROBE LLM NOW` button runs a 1-token Claude round-trip.
    - **Global LlmHealthBanner**: red banner above Nav on every page polling `/api/llm-health` every 60s; auto-shows when degraded, dismissable per-session.
    - **BIG BANG launch campaign** (`LaunchCampaignPanel.jsx`): 28-day organic-growth playbook with 21 pre-written JADE-voice posts across 11 platforms (LinkedIn, X, IG Reels/Feed, TikTok, YouTube/Shorts, Facebook, Reddit, Hacker News, Threads). 4 phases (TEASE / REVEAL / PROOF / CONVERT). One-click COPY POST. 5 re-encoded video assets ready to drop into each platform: 9:16 vertical (TikTok/Reels/Shorts), 1:1 square (IG/LinkedIn), 16:9 landscape (X/YouTube), poster JPG, audio MP3. Served via `/api/launch/asset/{name}` (public). Backend `/app/backend/launch_campaign.py`.
    - **Security hardening**: SecurityHeadersMiddleware adds HSTS / X-Content-Type-Options / X-Frame-Options / Referrer-Policy / Permissions-Policy / CSP base headers to every response. In-memory token-bucket rate limit (8 attempts / 5 min per IP) on `/api/auth/login`. PDF upload now requires `%PDF` magic-byte prefix + denied-pattern scan for `/JavaScript /Launch /EmbeddedFile`. Path-traversal blocked on `/api/launch/asset/{name}`.
- **UX REVAMP — ATLANTEAN MOOD + JADE AVATAR + SAVE/PRINT (2026-02-05)** — shipped & verified end-to-end:
    - **MoodPicker** (bottom-left widget) — 4 themes (CONSOLE / ATLANTEAN / WARM / HI-CONTRAST) toggling `html[data-theme]`. Persists to `localStorage.jadeos.theme`. Theme overrides flow through `--jade-lime`, `--quantum-cyan`, `--vault-violet`, `--console-black` CSS vars; `.bg-console` / `.bg-console-2` / `.bg-card-deep` now bind to these vars so theme actually propagates visually. WARM is the older-eyes amber palette; HI-CONTRAST is the accessibility option.
    - **JadeAvatar / JadeWorking** — glowing holographic Jade-lime orb with rotating dashed rings, scanline sweep, corner brackets, pulse motes, breathing animation. `<JadeWorking verb="extracting">` is the drop-in replacement for any "loading…" spinner. Wired into AgentDemo Chat (per-message), Dashboard SelfTest run state, Email Draft modal, and Dashboard Leads polling.
    - **SaveActions** — `↓ SAVE` (downloads `.txt`/`.json` blob with timestamped filename) + `⎙ PRINT` (browser print dialog using existing `@media print` styles that hide nav/footer/mood-picker, light backgrounds, black text). Mounted in AgentDemo (Extract/Outreach/Qualify/Support outputs), Dashboard SelfTest report, Email Package modal, PlaybookBuilder test result. `data-print-hide` attribute used to scope hide behavior during print.
    - Verified via testing agent — 15/15 frontend checks passing; backend `/api/promo/video?v=3` + `/api/promo/meta` + `/api/auth/login` all confirmed.
- **PROMO REEL V3 — SHIPPED (2026-02-05)** — sora-2-pro split-screen real-ops reel: Outlook inbox auto-sorting + Freight BOL extraction + Support ticket triage, all glowing back to central JADE Quanta, with first 12s of user-supplied JADE TTS voice muxed via ffmpeg. `/app/static/jadeos_promo_v3.mp4` (6.89 MB, 12s, 1280×720). Served via `/api/promo/video?v=3` and as the default reel (highest version wins). Dashboard PROMO REEL tab now has `V3 · SPLIT OPS` toggle. Sora video_id captured in `/app/static/jadeos_promo_v3.json` so it can be re-downloaded without regeneration cost via `backend/scripts/download_promo_v3.py <video_id>` — useful when the budget gate flaps mid-pipeline (as happened during this session: video generated successfully but the download endpoint ran a budget recheck that briefly blocked retrieval until balance was topped up).
- **JADE GENESIS + PRESS KIT (2026-02-04)** — user-supplied Genesis self-portrait ingested as the official "face of JADE". New reusable `<JadeGenesisCard />` component (corner brackets + lime LIVE pill + GENESIS provenance caption + "Jade." wordmark + 3 CTAs: Ask Jade → /demo, Build a playbook → /playbooks/new, Claim a pilot → /lighthouse). Placed tastefully on the **Landing page** as section 01 ("This is Genesis. The first image Jade made of herself.") and headlining the new **Press Kit page** at `/press`. Footer now links to Press Kit. **Press Kit / Marketing Pack** (`/press`) bundles: hero, Meet Jade w/ Genesis card, downloadable brand assets grid (Genesis, Quanta, banner, 3 logos · click-to-download), 5-color brand palette w/ click-to-copy hex, embedded JADE voice sample (the 4.6min audio file), embedded promo reel + MP4 download, key facts (founder/region/stack/pricing), 3 approved copy-pasteable one-liners, and a contact card (Oliver / cummins_oliver@yahoo.com / onejades.com).
- **AUTO-FOLLOWUP PIPELINE (2026-02-04)** — every Lighthouse intake (web form OR Twilio voice OR Twilio SMS) now auto-fires (a) a follow-up SMS to the captured phone with demo reel URL + lighthouse claim link, and (b) a welcome-email package with score, fit assessment, demo links, pricing, plain-English "what JADE does today" summary, signed by Oliver. If Twilio/Resend not configured, each followup is **queued to `db.auto_followups`** with reason — once keys arrive, retry endpoint fires them. Backend: `POST /api/auto-followups/trigger` (manual fire by application_id), `GET /api/auto-followups` (list w/ channel + status filters), `POST /api/auto-followups/{fid}/retry`. Voice IVR now uses Joanna voice (matches the female JADE TTS we're integrating). End-to-end smoke test verified — 1 web Lighthouse submission produced 2 queued followups (sms+email) with correct, real content.
- **JADE VOICE FILE INGESTED (2026-02-04)** — user-supplied `09-jade-survival-codex.mp3` (4.6 min, female TTS, premium quality, identifies itself as JADE) downloaded to `/app/backend/audio/jade_voice.mp3`. First 12s pre-extracted to `jade_voice_12s.mp3` (with fade-out) — ready to mux onto v3 video. Voice analysis (Gemini): "confident, authoritative, premium TTS — excellent fit for B2B SaaS operator-grade brand."
- **URLs swept to onejades.com (2026-02-04)** — replaced all user-visible `jadeos.ai` references (PlaybookPanel, PitchDeck case-study footer, PlaybookBuilder default owner_email). Internal IDs (admin auth email, sms+/voice+ stored identifiers, test fixtures) preserved.
- **PROMO REEL V3 — BLOCKED**: superseded — see V3 SHIPPED entry above (2026-02-05).
- **PROMO REEL V2 (2026-02-04)** — regenerated via **sora-2-pro** (premium model), 12s · 1280×720 · 7.57 MB. New beat-structured prompt features: (1) matte-black titanium Quanta keyfob with lime-LED edges + circuit-board windows + fingerprint scanner (animated, pulses, projects hologram), (2) JADE OS wordmark holographic projection, (3) four orbiting UI cards showing real ops scenes — `EMAIL · AUTO-SORTED · 184 TODAY` (Outlook-style chaos → folder routing), `TICKETS · TRIAGED · 38s AVG`, `LEADS · SCORED · 1.2k MTD`, `DOCS · EXTRACTED · 2.4k MTD`, (4) industry sigil montage (truck/stethoscope/gear/gavel/document/dollar) orbiting Quanta, (5) wordmark reveal with `AI AGENTS FOR THE OPERATOR · ONEJADES.COM`. Backend endpoints `/api/promo/video` and `/api/promo/meta` now support `?v=1|2` version pinning and default to newest. Admin PROMO REEL tab shows V2/V1 toggle. Production script: `backend/scripts/generate_promo_video_v2.py`.
- **PROSPECTS UI (2026-02-04)** — wired into Leads tab. AI generates MSP-area B2B prospects per industry (LLM-synthesized via Claude with industry-specific MSP geographic hints), filterable, with one-click "DRAFT EMAIL" that opens a tailored solicitation package modal (subject + 90-140 word operator-grade body + 3 talking points + PS) and a single OPEN IN EMAIL CLIENT button that fires `mailto:` with subject/body pre-filled — auto-marks contacted on click. COPY SUBJECT / BODY / ALL also available. Backend endpoints already shipped: `/prospects/generate`, `/prospects`, `/prospects/{id}/email-draft`, `/prospects/{id}/contacted`, DELETE.
- **PROMO REEL · Sora 2 (2026-02-04)** — Sora 2-generated 12s · 1280×720 high-tech promotional video clip with full JADE OS branding (lime/cyan/violet on console black, holographic interface, industry sigils orbiting bracketed monogram, wordmark reveal). Saved to `/app/static/jadeos_promo.mp4` (9.06 MB · rendered in 169s). Backend: `GET /api/promo/video` (FileResponse) + `GET /api/promo/meta`. New `PROMO REEL` tab in admin with embedded player, one-click DOWNLOAD MP4 / COPY PUBLIC URL / COPY SOCIAL CAPTION, platform-routing tips, and the raw Sora 2 prompt for re-rendering. Production script: `backend/scripts/generate_promo_video.py`.
- **LIGHTHOUSE CRASH FIX (2026-02-04)** — `<Stat>` component was referenced in `LighthousePanel` but never defined → `ReferenceError` crashed the entire admin Lighthouse tab. Added the `Stat` helper alongside `SummaryStat`. Tab now renders cleanly.
- **P0 AUTH GATES (2026-02-04)** — `POST /api/billing/portal-session` and `POST /api/playbooks/customer` now require `require_admin` (returns 401 unauthenticated). Closes the P0 surface flagged in iteration 3.
- **PROSPECTS · backend (2026-02-04)** — LLM-generates realistic Minneapolis-area B2B prospects per industry (`POST /api/prospects/generate`), lists/deletes/marks-contacted, and drafts tailored solicitation email packages (`POST /api/prospects/{id}/email-draft`). Frontend wiring still pending.
- **ADMIN SELF-TEST (Round 5 · 2026-02-04)** — new `SELF-TEST` tab in admin/console runs a battery of 21 checks across every major feature (auth, leads, lighthouse, MOAT schemas/prompts/playbooks, KB, webhooks, billing/Stripe, Twilio SDK + config, LLM key, Claude chat round-trip, Claude extract+JSON, agent_runs ledger, case studies, pypdf, Mongo ping). Each check returns `{name, category, status, latency_ms, message, details}`. `?deep=true` toggles LLM round-trips (cost-gated). UI: per-category result groups with PASS/FAIL/SKIP counters, expandable rows showing raw JSON, summary cards. Backend endpoint: `GET /api/admin/self-test` (admin-gated). Regression: `backend/tests/test_self_test.py` (2/2 passing).
- **THE MOAT (Round 4)** — customer-locking IP layers that turn JADE from "thin LLM wrapper" into a defensible product:
    - **Schema Library** — 4 seeded versioned extraction schemas (freight_bol, healthcare_intake, saas_order_form, manufacturing_po). Customer corrections increment `correction_count` → our schemas improve from THEIR data.
    - **Prompt Library** — 3 seeded named/versioned prompts with A/B variants. `/api/prompts/run` interpolates `{{vars}}` and routes through `_route_model`.
    - **Playbooks** — multi-step workflows orchestrated as code (not Zapier-able). 3 seeded: freight_load_intake (extract→outreach), healthcare_intake_triage (extract→triage), saas_inbound_lead (qualify→outreach). Verified end-to-end run: 12.8s, 2 steps, both green.
    - **Model Router** — `MODEL_ROUTING` dict with 3 profiles (fast/default/smart). Customer code never changes when prices shift.
    - **Moat Analytics** — `/api/moat/stats` (public) + `/api/moat/admin` (auth) surface the accumulating IP.
- **LIGHTHOUSE CUSTOMER PROGRAM (Round 4)** — `/lighthouse` dedicated landing with multi-section application form. Applications auto-scored by JADE's own lead-qualification agent (dogfooding). Hot applicants auto-advance to 'screening' status. 5-seat cap with public counter. Admin dashboard has a new Lighthouse tab sorted by JADE score, expandable rows with rationale + flags + status workflow (new → screening → interview_scheduled → selected → pilot_live → case_published).
- **Public**: + Lighthouse band on landing page driving CTR to /lighthouse.
- **Backend**: **86/86 tests passing** across 3 iterations.
- Admin credentials in `/app/memory/test_credentials.md`.

## Backlog
- **P1** (NEXT): Refactor `server.py` (~4200 lines) into modular `APIRouter`s — extract `routers/rag.py`, `routers/client_auth.py`, `routers/lighthouse.py`, `routers/billing.py`. Backend reviewer flagged it twice now.
- **P1**: Production wire-in of scaffolds when the operator drops keys — Sentry DSN, OpenAI key for real embeddings, Resend API key. Each is one .env line away.
- **P2**: APScheduler for unattended autonomous agent workflows. · Spend tracking + budget alerts (50/80/100% thresholds). · Twilio voice/SMS agent · Stripe-hosted customer portal · per-customer hard token caps. · Rate-limit `/api/rag/query` (it's public for the agent runtime).
- **P3**: pgvector / pinecone provider implementation in `rag_store.py` · embed-anywhere reel widget · multi-user org roles · RBAC on client portal.
